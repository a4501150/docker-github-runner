services:
  runner:
    build: .
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/data
    group_add:
      - "${DOCKER_GID:-0}"
    restart: unless-stopped
